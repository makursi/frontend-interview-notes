# AGENTS.md

前端面试知识库（个人备考笔记）：**纯 Markdown 文档，无任何可运行代码**。

## 语言与内容约定

- 所有文档均为简体中文；新增或修改内容必须用中文写，保持面试答案式风格。
- 每篇文档的常见结构：`#` 标题 = 完整知识点名 → `## 一、二、三…` 中文序号小节 → `###` 细分点 → 关键结论用 `> ` 引用块开头（如 `> 核心结论`）。
- 代码一律放在 ```` ```js ```` 围栏内，输出写在 JS 注释里；文中常用 ⚠️/❌/✅ 标记易错点与结论（不要使用其他表情符号）。
- 内容面向**面试答题**：覆盖高频考点、易错陷阱、对比表格、手写实现，不要写成教程式长篇。

## 目录结构

| 目录 | 内容 |
|------|------|
| `JavaScript核心/` | this、闭包、事件循环、Promise、原型链等；`ES6特性/` 下为编号文件 `知识点1.md`…`知识点6.md` |
| `手写题/` | 手写实现题（深拷贝、防抖节流、Promise.all、new 等），每篇一题 |
| `CSS基础/` `浏览器原理/` `计算机网络/` `框架/` `工程化/` `算法与数据结构/` `性能优化/` | 对应主题的面试知识点，按主题归类 |
| `public/` | 文档配图目录（`javascript/prototype.webp`），当前无 markdown 引用，为遗留资源 |

## 操作注意事项

- 无 build/test/lint/typecheck。`package.json` 的 `test` 脚本是占位（`echo "Error: no test specified"`，故意失败），不要运行或依赖它。
- 该目录**不是 git 仓库**（父级 `W/` 也不是），不要执行 git 操作。
- 存在占位/空文件（如 `工程化/代码规范.md`、`README.md`、部分 `手写题/*.md` 只有一行标题）——表示待完善，不要删除。
- 文件命名：描述性中文名，允许含空格（如 `Promise - Promise.all.md`、`Vite 原理与 Webpack 的区别.md`）。新增文件沿用此风格，先检查是否已有同主题文档，避免重复建题。
- 不新增 package.json scripts；该包位于 `W/apps/` 下，`type: "module"`、零依赖（根目录尚无 `pnpm-workspace.yaml`，工作区未真正启用）。
